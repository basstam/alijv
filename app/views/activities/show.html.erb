<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('activerecord.attributes.activity.name') + ':' %></strong>
  <%= @activity.name %>
</p>

<p>
  <strong><%= t('activerecord.attributes.activity.description') + ':' %></strong>
  <%= @activity.description %>
</p>

<%= link_to t('helpers.navigate.edit'), edit_activity_path(@activity) %> |
<%= link_to t('helpers.navigate.back'), activities_path %>
